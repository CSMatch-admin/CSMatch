% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_overlap_stat.R
\name{calculate_overlap_statistics_from_table}
\alias{calculate_overlap_statistics_from_table}
\title{Calculate Overlap Statistics from Matched Table}
\usage{
calculate_overlap_statistics_from_table(full_matched_table)
}
\arguments{
\item{full_matched_table}{A data frame containing the matched data.}
}
\value{
A list containing all three sets of overlap statistics
(\code{pairwise_overlap}, \code{control_reuse}, \code{shared_per_treated}). Note
this is a different, larger shape than the similarly-named
\code{\link[=calculate_overlap_statistics]{calculate_overlap_statistics()}} returns, which only returns the
\code{pairwise_overlap} piece for backward compatibility — see
\code{dev/FUTURE_WORK.md} for a plan to align these.
}
\description{
Calculates all three overlap metrics for a matched dataset from a
pre-existing full matched table.
}
\examples{
set.seed(4044440)
dat <- gen_one_toy(nt = 5)
mtch <- get_cal_matches(dat,
                        metric = "maximum",
                        scaling = c(1/0.2, 1/0.2),
                        caliper = 1,
                        rad_method = "adaptive",
                        est_method = "csm")
calculate_overlap_statistics_from_table(result_table(mtch, nonzero_weight_only = TRUE))

}
