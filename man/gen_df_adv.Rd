% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_data.R
\name{gen_df_adv}
\alias{gen_df_adv}
\title{Generate toy data}
\usage{
gen_df_adv(
  nc,
  nt,
  f0_sd = 0.1,
  f0_fun = function(X1, X2) {
     1
 },
  tx_effect_fun = function(X1, X2) {
     1
 },
  f0_sd_fun = NULL,
  cluster_dist = 0.5,
  prop_nc_unif = 1/3
)
}
\arguments{
\item{nc}{(something)}

\item{nt}{(something)}

\item{f0_sd}{degree of residual noise (default homoskedastic)}

\item{f0_fun}{(something)}

\item{tx_effect_fun}{(something)}

\item{f0_sd_fun}{Function to generate noise standard deviation.
Default is NULL. If not null, override f0_sd.}

\item{cluster_dist}{Cluster separation, from 0 to 1; lower means
better overlap}

\item{prop_nc_unif}{proportion of uniform controls. lower means
worse overlap}
}
\value{
A tibble containing a toy dataset. \code{Z} is numeric (0/1).
Also includes the \code{Y0_denoised}/\code{Y1_denoised}/\code{Y_denoised}
(noiseless potential outcome) columns that \code{\link[=gen_df_adv_k]{gen_df_adv_k()}}
produces.
}
\description{
generate data mostly in grid (0,1) x (0,1)
}
\details{
This is the fixed-2D-covariate special case of \code{\link[=gen_df_adv_k]{gen_df_adv_k()}}
(\code{k = 2}), implemented as a thin wrapper around it so the two share
one underlying DGP implementation. \code{f0_fun}/\code{tx_effect_fun}/
\code{f0_sd_fun} here use the \verb{function(X1, X2)} calling convention
(rather than \code{gen_df_adv_k()}'s single k-column-matrix
\verb{function(X)}), for backward compatibility with existing callers.
}
\examples{
dat <- gen_df_adv(nc = 100, nt = 10)
dim(dat)
}
